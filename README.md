# 🏠 膳待家 Umami

「膳食 · 待在家 · 管家」——基于 [pi](https://github.com/earendil-works/pi)（`pi-ai` + `pi-agent-core`）构建的**本地优先膳食健康管家**。覆盖饮食、健身、身体数据与习惯管理；健康记录和会话持久化到本地 SQLite，选择云模型时只向对应提供商发送完成当前请求所需的上下文。

> ⚡ **随身轻量版**：膳待家也有 Agent Skill 版——装进 ZCode / Codex / Claude Code，在聊天窗口里就能管冰箱、出菜谱、记三餐、排训练，数据同样存本地 SQLite。见 [Umami-Skill for Health](https://github.com/renchenxuan/Umami-Skill-for-Health)。**要界面用本 Web 版，图顺手装 Skill 版。**

## 为什么做这个项目

程序员的一天，常常从一杯咖啡开始，在深夜的屏幕前结束。久坐、外卖、不规律——「保持好身材」成了许多人愿望清单上年年出现、又年年落空的一项。我们不是不想健康，只是缺一个够得着、懂生活的小帮手。

「膳待家」就是为程序员（以及所有久坐的人）做的健康管家。基于 pi-agent 构建，`bun run dev` 一键启动、打开浏览器即用：拍一下冰箱帮你出菜谱，聊两句帮你排训练，随手记体重、打卡喝水睡觉。所有数据都存在本地 SQLite，不注册、不上传，隐私归你自己。

项目目前处于 **v3.1** 阶段，功能还在快速打磨中，难免粗糙——欢迎指正、提意见（Issue / PR 都欢迎）。希望它能陪你做一个更健康的程序员。

## 功能特性

- 📸 **拍照识别食材**：上传冰箱照片，视觉模型识别并保存食材
- 📋 **菜谱生成 / 🛒 购物清单 / 📊 营养估算**：展示份量假设与不确定性，不替代营养数据库
- 🔢 **卡路里量化记录**：内置 165 种常见食材营养表（每 100g 热量/蛋白/脂肪/碳水）+ 克数换算（一碗米饭≈200g 熟重），记一餐自动算总热量；饮食页有目标进度环（按身体数据推算 TDEE，可手动覆盖）、三餐分布与 7 天趋势；菜谱/分析提示词要求透明标注份量假设
- 🏋️ **训练计划与动作指导**：按目标、器械、天数生成训练计划，讲解动作要领
- 📏 **身体数据追踪**：记录体重/体脂，追踪减脂增肌进度
- 🎯 **目标管理**：减脂、增肌、睡眠、饮水等目标设置与状态跟踪
- 💧 **习惯打卡**：睡眠、饮水、心态等日常习惯记录
- ⏰ **定时任务/自动化**：聊天里说「每天晚上六点提醒我吃晚饭」即可建任务，到点提醒自动写进会话并由 AI 回应，支持每天/每周/单次与启停管理
- 🍙 **桌宠「团团」**：饭团小厨吉祥物常驻聊天区，会打瞌睡、陪你等 AI 回复、为确认成功欢呼，可拖拽逗弄（改写自 MIT 开源的 [dsh-kun-like-pet](https://github.com/liyupi/dsh-kun-like-pet) 引擎，精灵图见 `tools/pet-atlas/` 的确定性生成管线）
- 🌅 **动态氛围**：AI 思考时极光呼吸加速、厨房台面剪影随晨昏亮灯、蒸汽与光尘粒子、操作成功涟漪反馈，支持三档氛围开关与 reduced-motion 降级
- 💬 **多会话**：聊天按会话隔离，列表卡片化（最后消息预览/相对时间/今日昨天分组/搜索），可新建 / 切换 / 删除
- ✍️ **写操作确认**：食材/训练/身体/目标/习惯等写操作先产出待确认提案，确认后才写入，支持取消与撤销
- 🔍 **数据流向透明**：每个 AI 功能都写明实际发送的数据类别（推荐含冰箱食材、分析含健康目标与估算热量），结果标注估算/推测，未建档时明示为普适建议；「AI 保鲜建议」按记录日期推测、非传感器测量
- 🍳 **开小灶（烹饪教学）**：内置 36 道家常菜分餐次教程（食材克数、切配、火候步骤、易错点），也可以让 AI 生成或自己写一份，全部支持编辑；详情收起自动回到列表
- 🗺️ **外部服务与户外训练规划**：设置中心「模型｜外部服务」双页签管理，支持百度/高德/谷歌三选一连接（密钥只存系统凭据）；连接后 AI 可搜附近公园操场、测算步行/骑行路线距离耗时，用于落地训练计划，未连接则不编造路线
- 📱 **移动端原生导航**：底部标签栏不裁切、会话抽屉、「我的冰箱」库存前置，看板卡片自动瀑布流排布（拖拽过的卡片保留位置）
- 🛡️ **健康指导 Skill**：个性化推荐、过敏硬约束、医疗信息边界与紧急警示由主 Agent 和推荐接口共享
- 🔌 **多模型支持**：OpenAI / Gemini / DeepSeek / Moonshot(Kimi) / MiniMax / Anthropic / 通义千问 / 智谱 GLM；自定义 OpenAI 兼容端点默认禁用，仅允许显式开启后的公网 HTTP(S) 地址
- 🎨 **分区氛围视觉**：emerald 品牌色 + 每板块专属点缀色（健身珊瑚橙 / 饮食暖金 / 身体冷蓝 / 冰箱青绿），分层渐变背景随页面柔和过渡，深/浅双主题，71 张食材实拍图

## 技术栈

- **运行时**：Bun（`bun:sqlite` + `Bun.serve`）
- **Agent 框架**：`@earendil-works/pi-agent-core`
- **统一 LLM 接口**：`@earendil-works/pi-ai`
- **数据存储**：SQLite

## 快速开始

### 1. 安装依赖

```bash
bun install
```

### 2. 启动

```bash
bun run dev
```

浏览器打开 <http://localhost:3000>。

### 3. 配置模型（网页内完成）

点击右上角 **⚙️ 设置中心**：选择模型提供商 → 填写 API Key → 「测试连接」→ 「保存」。切换模型即时生效，不丢会话。

选「自定义（OpenAI 兼容）」时需额外填写 **Base URL** 和 **模型 ID**。出于 SSRF 与重定向风险，自定义网络端点默认禁用；只有明确设置 `ALLOW_UNSAFE_CUSTOM_ENDPOINTS=true` 后才可使用，且地址必须解析到公网 HTTP(S)，不能指向本机或私有网络。

> 也可以复制 `.env.example` 为 `.env` 预填。新 Key 不写入 SQLite：Windows 使用 Credential Manager，其他平台仅支持环境变量回退。请勿提交 `.env` 或 `*.db`。

## 使用示例

**饮食**：「我冰箱里有鸡蛋和西红柿，给 2 个人做一顿晚餐」／（上传照片自动识别食材）／「分析一下这些菜的营养」

**健身**：「给我制定一个 3 天的减脂训练计划，无器械」／「记录我今天慢跑 30 分钟」／「我最近的训练记录」

**身体**：「记录体重 70kg」／「我的体重变化」

**目标与习惯**：「设置目标：减脂到 65kg」／「昨晚睡了 7 小时」／「喝水打卡」

## 项目结构

```
├── package.json
├── tsconfig.json
├── .env.example
├── tools/pet-atlas/         # 团团精灵图的确定性生成管线（SVG 帧 → Playwright → Pillow）
└── src/
    ├── index.ts              # 入口：装配并启动（含定时任务调度器）
    ├── config.ts             # 静态配置（端口/路径/模型 ID）
    ├── settings.ts           # 运行时模型设置 + 凭据存储适配
    ├── secrets.ts            # 密钥存储（Windows 凭据管理器 / 环境变量）
    ├── models.ts             # 9 家模型 provider + 自定义 OpenAI 兼容端点
    ├── agent.ts              # Agent 组装
    ├── system-prompt.ts      # 膳待家人设
    ├── skills/               # Agent Skill（个性化推荐与健康安全边界）
    ├── api-types.ts          # v1 API 通用类型（ApiResult / SSEEvent，含 agent_state 与 schedule_fired）
    ├── prompts/nutrition.ts  # 营养分析提示词
    ├── db/database.ts        # SQLite 数据层（迁移至 v11：schedules、食物营养列与开小灶内置教程）
    ├── db/tutorial-presets.ts # 开小灶 36 道内置家常菜教程
    ├── tools/                # Agent 工具（食材/收藏/历史/偏好/营养/训练/身体/目标/习惯/定时任务/教学菜谱/地图）
    └── server/
        ├── server.ts         # Bun.serve + 设置接口 + 全局事件流 /api/v1/events
        ├── api.ts            # v1 REST API（CRUD / 会话 / 动作确认 / schedules / diet-summary）
        ├── conversations.ts  # 会话 Agent 管理 + SSE 流式 + 定时提醒写入
        ├── scheduler.ts      # 定时任务调度器（到期扫描 → 会话提醒 → 事件广播）
        ├── diet-summary.ts   # 热量目标推算（Mifflin-St Jeor）与每日/每周汇总
        ├── events.ts         # 服务端事件总线（定时任务等跨请求事件）
        ├── errors.ts         # 错误信息脱敏
        └── static/           # 聊天前端 + 设置面板 + 会话栏 + 桌宠引擎（pet.js）
```

## 架构说明

应用为**按会话隔离的 Agent + 一组确定性工具**：

- **结构化页面不依赖模型**：食材、训练、饮食与资料在无 Key 或断网时仍可查看和维护；
- **智能部分由 Agent 的 LLM 承担**：识别食材、生成菜谱/训练计划和通用健康信息；不提供医疗诊断或治疗建议；
- **工具只做确定性副作用**：读写 SQLite、营养分析子调用；
- **写操作先提案后确认**：会话中的写工具先生成「待确认提案」，用户确认后才真正写入，支持取消与撤销；
- **会话隔离与恢复**：每个会话持久化消息并拥有独立 Agent 上下文；请求开始时使用当前模型配置快照。

## 许可

MIT License
