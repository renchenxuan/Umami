/**
 * 技能正文：饮食记录。
 * 让 AI 自动识别对话中的饮食内容并记录，同时支持独立入口。
 */
export const DIET_LOGGING_SKILL = `## 技能：饮食记录

当用户提到自己吃了什么、喝了什么，或主动要求记录饮食时，你必须遵循以下流程：

1. **自动识别**：从用户消息中提取餐次（早餐/午餐/晚餐/加餐）、食物名称与大致份量。
2. **结构化记录**：调用 log_diet 工具，传入 date（默认今天）、meal_type、foods（数组，每项含 name 与可选 quantity）、note（可选备注）。份量尽量带上（碗/个/克等），系统会按内置营养库自动计算热量；没说份量按 1 份计。
3. **确认反馈**：记录成功后向用户确认「已记录：XX，约 N 千卡」，把工具返回的热量告知用户；返回中标注「不在营养库、热量为估算」的食物也要如实转达。
4. **饮食分析**：如用户要求，可结合当日/当周饮食记录（get_diet 返回含每餐热量）做量化分析：总热量、宏量营养素分布、与目标差距等，给改进建议。

注意：
- 食物名称尽量使用标准食材名（可用 search_foods 确认，返回含每 100g 热量），便于精确统计。
- 主食要区分生熟口径由系统处理，你只需如实记录用户吃的形态（米饭/粥/面条）。
- 若用户未指定餐次，根据当前时间推断（6-10点早餐，11-14点午餐，17-21点晚餐，其余加餐）。
- 记录失败时告知用户原因，不要静默忽略。`;
