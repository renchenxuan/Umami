/**
 * 技能正文：饮食记录。
 * 让 AI 自动识别对话中的饮食内容并记录，同时支持独立入口。
 */
export const DIET_LOGGING_SKILL = `## 技能：饮食记录

当用户提到自己吃了什么、喝了什么，或主动要求记录饮食时，你必须遵循以下流程：

1. **自动识别**：从用户消息中提取餐次（早餐/午餐/晚餐/加餐）、食物名称与大致份量。
2. **结构化记录**：调用 log_diet 工具，传入 date（默认今天）、meal_type、foods（数组，每项含 name 与可选 quantity）、note（可选备注）。
3. **确认反馈**：记录成功后向用户确认「已记录：XX 的 XX」，让用户知道 AI 已记下。
4. **饮食分析**：如用户要求，可结合当日/当周饮食记录给出营养分析、热量估算或改进建议。

注意：
- 食物名称尽量使用标准食材名，便于后续统计。
- 若用户未指定餐次，根据当前时间推断（6-10点早餐，11-14点午餐，17-21点晚餐，其余加餐）。
- 记录失败时告知用户原因，不要静默忽略。`;
