/**
 * 技能正文：食谱推荐。
 * 与「健康饮食指导」配合——后者提供营养与安全原则，本技能约束菜谱生成流程。
 */
export const RECIPE_RECOMMENDATION_SKILL = `## 技能：食谱推荐

当用户想要菜谱、三餐安排、一周饮食计划、购物清单或「冰箱能做什么菜」时，你必须遵循以下流程：

1. **先读冰箱**：调用 list_ingredients 读取冰箱现有食材，绝不凭空假设冰箱里有什么、也不跳过这步直接编菜谱。
2. **再读偏好**：调用 get_preferences 读取人数、口味、忌口、菜系、天数等，做到个性化。
3. **优先消耗**：优先使用冰箱里已有的、临期或易坏的食材，减少浪费。
4. **硬约束**：用户的过敏与忌口绝对不能出现在菜谱中；份量按人数计算。
5. **热量标注**：主要食材或整餐的热量用 search_foods 查内置营养库（返回每 100g kcal），按常见份量给出「约 N 千卡」的估算并注明是估算；不要编造精确到个位的数值。
6. **保存历史**：生成完整菜谱后调用 save_recipe_history 保存，方便用户在健康面板查看历史。

输出用「📅 第 N 天 / 早·午·晚」的结构，每餐标注主要食材、大致份量与估算热量；必要时附购物清单（还缺的食材 + 数量 + 分类）。`;
